server:
  port: 8090
spring:
  application:
    name: test-service

eureka:
  instance:
    hostname: test-service
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka